#!/bin/sh
#Installation of Libsodium
echo "$MSG_SODIUM"

git clone https://github.com/jedisct1/libsodium.git -b master
cd libsodium
autoreconf -vi
./configure --prefix=$WORKDIRECTORY --disable-shared && make -j$CPU_NUM
make install
cd ..
